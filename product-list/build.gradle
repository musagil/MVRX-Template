apply from: configFile('android-ui-library-config.gradle')

android {
    resourcePrefix 'product_list_'
}

dependencies {
    implementation project(':product-data')

    kapt deps.moshi.codegen
    implementation deps.moshi.core
}
